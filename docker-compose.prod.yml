services:
  api-gateway:
    build:
      context: .
      dockerfile: infra/production/api-gateway.Dockerfile
    ports:
      - "8081:8080"
    environment:
      - GO_ENV=PRODUCTION
    restart: unless-stopped
