docker_compose("./docker-compose.yml")

dc_resource(
    'web',
    labels=['frontend']
)

dc_resource(
    'api-gateway',
    labels=['api']
)

dc_resource(
    'collection-db',
    labels=['database']
)

dc_resource(
    'device-db',
    labels=['database']
)

dc_resource(
    'collection-service',
    labels=['microservice']
)

dc_resource(
    'collection-simulator',
    labels=['test']
)

dc_resource(
    'device-service',
    labels=['microservice']
)

dc_resource(
    'auth-service',
    labels=['microservice']
)

dc_resource(
    'chirpstack',
    labels=['chirpstack']
)

dc_resource(
    'chirpstack-gateway-bridge',
    labels=['chirpstack']
)

dc_resource(
    'chirpstack-rest-api',
    labels=['chirpstack']
)

dc_resource(
    'chirpstack-db',
    labels=['chirpstack']
)

dc_resource(
    'redis',
    labels=['chirpstack']
)

dc_resource(
    'mosquitto',
    labels=['chirpstack']
)
